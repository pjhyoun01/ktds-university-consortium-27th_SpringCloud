<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.userservice.dao.UserDao">

    <insert id="insertNewUser" parameterType="RegistUserVO">
        <selectKey order="BEFORE" resultType="string" keyProperty="userId">
        SELECT 'USR-' || TO_CHAR(SYSDATE, 'YYYYMMDD-') ||
        LPAD(SEQ_USERS_PK.NEXTVAL, 6, '0')
            FROM DUAL
        </selectKey>
        INSERT INTO USERS (USER_ID, EMAIL, PWD, SALT, NAME, CREATED_AT) VALUES
        (#{userId}, #{email}, #{pwd}, #{salt}, #{name}, SYSDATE) </insert>

    <select id="selectOneUserByUserId"
        parameterType="string"
        resultType="ResponseUserVO">
        SELECT USER_ID, EMAIL, PWD, SALT, NAME, CREATED_AT
        FROM USERS
        WHERE USER_ID = #{_parameter}
    </select>

    <select id="selectAllUsers"
        resultType="ResponseUserVO">
        SELECT USER_ID, EMAIL, NAME, CREATED_AT
        FROM USERS
    </select>

    <select id="selectOneUserByEmail"
        parameterType="string"
        resultType="ResponseUserVO">
        SELECT USER_ID, EMAIL, PWD, SALT, NAME, CREATED_AT
        FROM USERS
        WHERE EMAIL = #{_parameter}
    </select>


</mapper>